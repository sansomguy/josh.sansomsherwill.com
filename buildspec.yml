version: 0.2
phases:

  install:
    commands:
      - apt-get -qq install hugo

  build:
    commands:
      - hugo -F

  post_build:
    commands:
      - aws s3 sync public s3://josh.sansomsherwill.com
      - bash deploy.sh